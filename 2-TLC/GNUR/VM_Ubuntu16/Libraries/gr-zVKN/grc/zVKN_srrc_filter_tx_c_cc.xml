<?xml version="1.0"?>
<block>
  <name>SRRC Filter TX [C-CC]</name>
  <key>zVKN_srrc_filter_tx_c_cc</key>
  <category>zVKN</category>
  <import>import zVKN</import>
  <make>zVKN.srrc_filter_tx_c_cc($sps, $span, $beta)</make>
  <!-- Make one 'param' node for every Parameter you want settable from the GUI.
       Sub-nodes:
       * name
       * key (makes the value accessible as $keyname, e.g. in the make node)
       * type -->
  <param>
    <name>sps</name>
    <key>sps</key>
    <type>int</type>
  </param>

  <param>
    <name>span</name>
    <key>span</key>
    <type>int</type>
  </param>

  <param>
    <name>beta</name>
    <key>beta</key>
    <type>float</type>
  </param>

  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <sink>
    <name>in</name>
    <type>complex</type>
  </sink>

  <!-- Make one 'source' node per output. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <source>
    <name>out</name>
    <type>complex</type>
  </source>
</block>
